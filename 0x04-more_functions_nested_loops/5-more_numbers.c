#include "main.h"

/**
 * more numbers - prints 0-14 ten times
 * followed by a new line
 *
 * Return: void
 */

void more_numbers(void)
{
	int times, digits;

	for (times = 0; times < 10; times++)
	{
		for (digits = 0; digits <= 14; digits++)
		
		{
			if (digits >= 10)
				_putchar
}

